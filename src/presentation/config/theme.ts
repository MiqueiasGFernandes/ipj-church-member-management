import { createTheme } from '@mui/material';

export const customTheme = createTheme({
  palette: {
    primary: {
      main: '#9c1421ff',
    },
    secondary: {
      main: '#9c1421ff',
    },
  },
});
